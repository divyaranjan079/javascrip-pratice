let num = [1,2,3,4]

let newarray= num.map ((val)=>{
    return(val*val)
})
console.log(newarray);

// foreach is use to find some value and do some calculation...where as map is used to create a new array result from and exsting array...
